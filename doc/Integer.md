# Integer

- s**i**gned
	- `i8` `i16` `i32` `i64`
- **u**nsigned
	- `u8` `u16` `u32` `u64`
- number after `i/u` prefix is a bit-width of the integer
- the bit number is a $2^n$
	- starting from a single byte
	- as a common smallest addressable unit in a hardware (minimal CPU register)
